version: 2

models:
  - name: metricflow_time_spine
    description: "Date spine table required by MetricFlow. One row per day from 2019-01-01 to 2026-12-31."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
